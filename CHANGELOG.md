# 更新记录

## 0.2.1 - 2026-09-12

### 修复

- 桌面端正开着原对话时，`exec resume` 会因 “active writer” 失败；失败曾被当成续跑成功并记入 1 次上限，之后不再重试。现在会改成往原对话排队发消息
- 两个 `watch` 同时跑时，同一条对话可能被续两次。现在只允许一个 watch
- 占线判断只看这一次输出，不再被旧日志里的报错带偏

## 0.2.0 - 2026-09-11

### 新增

- 周额度用尽时，可自动使用 1 张 Codex Plus 重置卡，额度回来后仍在原来的 thread 续跑
- 5 小时额度用尽但周额度还在时，只等待 5 小时窗口，不会刷卡
- 刷卡前会再读一次额度；若已经恢复（例如提前刷新），则不扣卡
- 没有重置卡时不会盲打接口，继续等待周重置
- 刷卡失败后有约 10 分钟冷却，避免连打
- 可用配置项 `auto_redeem_weekly_reset` 关闭自动刷卡
- 续跑提示改为继续干活、不要停下来等人确认；已有配置里的旧默认文案会自动迁走
- `status` / `doctor` 可查看 5 小时额度、周额度和重置卡数量
- 增加模拟测试：假客户端覆盖“不扣卡 / 扣 1 张 / Tibo 已刷 / 没卡 / 冷却”等路径，不触碰真实扣卡接口

### 修复

- Windows 开机自启时，每隔约 30 秒闪一下 Terminal：不再调用 `tasklist`，也不再优先走 `codex.cmd`
- 后台改为无窗口拉起子进程，并复用同一次 Codex app-server 连接
- `doctor` 不再把重置卡 ID 整段打印出来
